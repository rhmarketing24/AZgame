import Link from 'next/link';
export default function Header(){ 
  return (
    <header style={{width:'100%'}}>
      <div style={{display:'flex',justifyContent:'center',padding:12}}>
        <h1 style={{margin:0}}>Tap A → Z Rush</h1>
      </div>
    </header>
  );
}
